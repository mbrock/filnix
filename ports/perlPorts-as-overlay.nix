# Perl package overrides
# Since we just want to use perl.withPackages with the cached perl,
# we don't need any package-level overrides
# Perl packages will be built with Fil-C automatically

pkgs: perlPackages: { }

